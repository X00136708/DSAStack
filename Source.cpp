#include <iostream>
#include "ListOfDoubles.h"
#include "StackOfDoubles.h"

using namespace std;

int main() {
	ListOfDoubles list;
	StackOfDoubles d;
	list.insert(1);
	list.insert(3);
	list.insert(5);
	list.insert(7);
	list.insert(9);
}